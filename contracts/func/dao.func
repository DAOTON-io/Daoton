() recv_internal() impure {
  slice ds = get_data().begin_parse();
  int dao_id = ds~load_uint(64);
  int contract_id = ds~load_uint(64);
  cell dict = ds~load_dict();
  set_data(begin_cell().store_uint(dao_id, 64).store_uint(contract_id, 64).store_dict(dict).end_cell());
}

(int, int, cell) get_total() method_id {
	slice ds = get_data().begin_parse();
 	int dao_id = ds~load_uint(64);
  int contract_id = ds~load_uint(64);
  cell dict = ds~load_dict();
	return (dao_id, contract_id, dict);
}
